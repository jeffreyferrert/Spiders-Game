
export default class Game {

    checkCollisions() {
        // between spider and blocks
        //between spider and bullets
        //between blocks and bullet
    }

    remove(element) {
        // remove if spider and bullet collision (x1)
        // remove if block and bullet collision (x4)
    }

    transformToBlock(pos){
        // transform to block if spider and bullet
    }

    goAway(el1,el2){
        // if two spiders colide they change their path
    }

}